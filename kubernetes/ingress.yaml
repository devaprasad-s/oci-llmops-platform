apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: manifest-doc-ingress
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: manifest-doc-service
            port:
              number: 8000
